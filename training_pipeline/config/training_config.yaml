base_model_name: meta-llama/Llama-3.2-3B-Instruct
output_dir: training_pipeline/checkpoints/llm-twin-lora

num_train_epochs: 3
batch_size: 2
learning_rate: 2e-4

lora:
  r: 16
  alpha: 32
  dropout: 0.05
  target_modules:
    - q_proj
    - k_proj
    - v_proj
    - o_proj
